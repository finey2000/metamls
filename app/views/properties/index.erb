<% provide(:title, "Metamls") %>
<main>
<%= render 'search_box'  %>  
<%= render 'sorting'  %>        
<%= render 'properties'  %>        
<%= render partial: 'pagination', locals: {add_query: "orderby=#{@order[:id]}"} %>          
</main>
